services:
  # PostgreSQL behaves like a real analytical warehouse target for dbt and JDBC.
  postgres:
    image: postgres:16.6-alpine
    environment:
      POSTGRES_USER: pipeline
      POSTGRES_PASSWORD: pipeline
      POSTGRES_DB: quality_lab
    ports:
      # A non-default host port avoids colliding with a developer's own database.
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pipeline -d quality_lab"]
      interval: 2s
      timeout: 3s
      retries: 20
    volumes:
      - postgres-data:/var/lib/postgresql/data

  minio:
    # MinIO provides the S3 API locally without requiring a cloud account.
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: pipeline
      MINIO_ROOT_PASSWORD: pipeline-secret
    ports:
      # API and browser console ports are distinct.
      - "19000:9000"
      - "19001:9001"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:9000/minio/health/live"]
      interval: 2s
      timeout: 3s
      retries: 20
    volumes:
      - minio-data:/data

  minio-init:
    # This one-shot service creates the bucket after MinIO is genuinely ready.
    image: minio/mc:RELEASE.2025-04-16T18-13-26Z
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - >
        mc alias set local http://minio:9000 pipeline pipeline-secret &&
        mc mb --ignore-existing local/pipeline-quality

  quality:
    # All Python, Spark, dbt, and test commands share one authoritative image.
    build:
      context: .
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: pipeline
      POSTGRES_PASSWORD: pipeline
      POSTGRES_DB: quality_lab
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: pipeline
      MINIO_SECRET_KEY: pipeline-secret
      MINIO_BUCKET: pipeline-quality
      DBT_PROFILES_DIR: /workspace/warehouse
      PYTHONPATH: /workspace/src
    depends_on:
      # Health-based dependencies prevent race conditions during local startup.
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      # Mounting the repository makes edits visible without rebuilding the image.
      - .:/workspace

volumes:
  # Named volumes keep local state between normal `up` and `down` operations.
  postgres-data:
  minio-data:
